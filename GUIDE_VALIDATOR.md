# 🛡️ Open-Vote Validator Node: ป้อมปราการของประชาชน

นี่คือหัวใจสำคัญของความเป็น **Decentralized** ในโครงการ Open-Vote เราเชื่อมั่นว่า "ความโปร่งใสที่แท้จริง ไม่ได้เกิดจากผู้สร้าง แต่เกิดจากผู้ตรวจสอบ" 

ทุกคนสามารถร่วมเป็นส่วนหนึ่งของประวัติศาสตร์การเมืองใหม่ ด้วยการรัน Validator Node เพื่อทำหน้าที่เป็น "ตาตรวจนับคะแนน" ที่เป็นกลางและเป็นอิสระ

---

## 🏗️ หน้าที่ของ Validator Node
Node ตัวนี้จะรันอยู่หลังบ้านเพื่อทำหน้าที่:
1.  **Monitor:** เฝ้าดูบล็อกเชน (Smart Contract) ตลอด 24 ชม. ว่ามีการอัปเดตผลคะแนน (Merkle Root) ชุดใหม่หรือไม่
2.  **Verify:** เมื่อตรวจพบข้อมูลใหม่ Node จะดึงข้อมูลคะแนนดิบมาจากฝั่ง Aggregator หรือ IPFS
3.  **Cross-Check:** ทำการคำนวณ Merkle Root ใหม่ด้วยตัวเอง (แบบอิสระ 100%) แล้วนำไปเปรียบเทียบกับค่าบนบล็อกเชน
4.  **Alert:** หากข้อมูลไม่ตรงกันแม้แต่ทศนิยมเดียว Node จะทำการแจ้งเตือนการทุจริตทันที!

---

## 🚀 วิธีการรัน Node สำหรับประชาชน (Community Node)

เราออกแบบมาให้รันได้ง่ายผ่าน **Docker** เพื่อให้ทุกคน (มหาวิทยาลัย, NGO, หรือประชาชนทั่วไป) สามารถทำได้ใน 3 นาที:

### 1. เตรียมความพร้อม
*   ติดตั้ง [Docker](https://www.docker.com/) และ [Docker Compose](https://docs.docker.com/compose/)
*   สมัครขอ RPC URL จาก Provider เช่น Alchemy หรือ Infura (เพื่อเชื่อมต่อบล็อกเชน)

### 2. สร้างไฟล์การตั้งค่า
สร้างไฟล์ `.env` ในเครื่องของคุณ:
```env
RPC_URL=https://polygon-amoy.g.alchemy.com/v2/YOUR_API_KEY
CONTRACT_ADDRESS=0x... (ตำแหน่ง Smart Contract หลัก)
AGGREGATOR_URL=https://aggregator.openvote.in.th
```

### 3. รันด้วย Docker Compose
สร้างไฟล์ `docker-compose.yml`:
```yaml
services:
  open-vote-validator:
    image: gridsmicro/open-vote-validator:latest
    environment:
      - RPC_URL=${RPC_URL}
      - CONTRACT_ADDRESS=${CONTRACT_ADDRESS}
      - AGGREGATOR_URL=${AGGREGATOR_URL}
    restart: always
```
จากนั้นพิมพ์คำสั่ง:
```bash
docker compose up -d
```

---

## 🤝 มาร่วมกันสร้าง "ความจริง"
ทุกๆ Node ที่เพิ่มขึ้น คือความยากลำบากที่เพิ่มขึ้นสำหรับผู้ที่คิดจะโกงการเลือกตั้ง การรัน Node ของคุณไม่ได้ใช้ทรัพยากรเครื่องมหาศาล (รันบน Raspberry Pi ได้) แต่มันคือการประกาศจุดยืนว่า **"คะแนนเสียงของประชาชน ต้องถูกต้องและห้ามใครแตะต้อง"**

> *"Power tends to corrupt, but Math is incorruptible."*
