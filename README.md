# Open-Vote : The Transparent Voting System
 โครงการ : Open-Vote (ระบบลงคะแนนเสียงสาธารณะเพื่อความโปร่งใส)

**Open-Vote** คือระบบลงคะแนนเสียงอิเล็กทรอนิกส์ที่ออกแบบมาเพื่อแก้ไขปัญหาความโปร่งใสในการเลือกตั้ง โดยใช้เทคโนโลยี **Blockchain** เป็นฐานข้อมูลหลักที่แก้ไขไม่ได้ (Immutable) เพื่อให้มั่นใจว่าทุกคะแนนเสียงจะถูกนับอย่างถูกต้องและตรวจสอบได้จากสาธารณะ

---

## 🌟 จุดเด่นของโครงการ (Core Pillars)

* **Open Source:** ซอร์สโค้ดทั้งหมดถูกเปิดเผย 100% เพื่อให้สาธารณชนตรวจสอบการทำงานได้
* **Immutable Ledger:** บันทึกคะแนนลงบนบล็อกเชน ทำให้ไม่มีใคร (รวมถึงผู้สร้างระบบ) สามารถแก้ไขผลคะแนนย้อนหลังได้
* **Public Verifiability:** ผู้ลงคะแนนสามารถใช้ Transaction ID เพื่อตรวจสอบได้ว่าคะแนนของตนถูกบันทึกเข้าระบบจริงหรือไม่
* **Decentralized:** ไม่ได้ยึดติดกับเซิร์ฟเวอร์ตัวกลางเพียงตัวเดียว ลดความเสี่ยงในการถูกโจมตีหรือแทรกแซงข้อมูล

---

## 🏗️ สถาปัตยกรรมระบบ (Tech Stack)

| ส่วนประกอบ | เทคโนโลยีที่ใช้ |
| :--- | :--- |
| **Blockchain** | Solidity (Smart Contract) |
| **Network** | Polygon / Ethereum Testnet |
| **Frontend** | React.js / Next.js |
| **Web3 Library** | Ethers.js / Wagmi |
| **Deployment** | Vercel / Docker |

---

## 🚀 ลำดับการทำงาน (How it Works)

1.  **Identity Verification:** ผู้โหวตยืนยันตัวตนผ่านระบบที่กำหนด (เช่น Web3 Wallet หรือ OTP)
2.  **Cast Vote:** เมื่อกดลงคะแนน ระบบจะสร้าง Transaction ส่งไปยัง Smart Contract
3.  **On-Chain Validation:** Smart Contract ตรวจสอบสิทธิ์ (เช่น ตรวจสอบว่าเคยโหวตไปแล้วหรือไม่)
4.  **Permanent Recording:** หากตรวจสอบผ่าน คะแนนจะถูกบันทึกลงในบล็อกเชนถาวร
5.  **Real-time Audit:** หน้า Dashboard จะดึงข้อมูลผลคะแนนรวมจากบล็อกเชนมาแสดงผลโดยตรง

---

## 🛠️ วิธีการติดตั้งเพื่อพัฒนา (Local Setup)

1. **Clone Repository**
  ```bash
  git clone https://github.com/GridsMicro/Open-Vote.git
  cd Open-Vote
  
  ```

2. **Install Dependencies**
  ```bash
  npm install
  
  ```

3. **Configure Environment**
สร้างไฟล์ `.env` และใส่ค่าตัวแปรต่างๆ เช่น RPC URL และ Contract Address

5. **Run Application**
  ```bash
  npm run dev
  
  ```

---

## 📜 ใบอนุญาต (License)

โครงการนี้อยู่ภายใต้ใบอนุญาต **MIT License** ซึ่งอนุญาตให้ทุกคนนำไปใช้ ศึกษา และต่อยอดได้โดยอิสระ เพื่อส่งเสริมความโปร่งใสในสังคม

---

> *"เพราะความเชื่อมั่นไม่ได้สร้างด้วยคำพูด แต่สร้างด้วยโค้ดที่ตรวจสอบได้"*

---
