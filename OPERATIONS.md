# 🛠️ Open-Vote Operations Manual: คัมภีร์การวางระบบประชาธิปไตยดิจิทัล

ยินดีต้อนรับสู่คู่มือการติดตั้งและบริหารจัดการระบบ **Open-Vote** โครงสร้างพื้นฐานการเลือกตั้งที่โปร่งใสที่สุดในยุคดิจิทัล

## 🏗️ สถาปัตยกรรมระบบ (System Architecture)
ระบบของเราถูกออกแบบมาให้ทำงานแยกส่วนกัน (Microservices) เพื่อความปลอดภัยและความกระจายอำนาจ:

1.  **Smart Contract (Blockchain):** หัวใจหลักในการเก็บ Root Hash ของคะแนนเสียง
2.  **Aggregator Service:** เครื่องยนต์รวบรวมโหนด ทำ Batching และส่งข้อมูลลงบล็อกเชน (รันโดยผู้จัดทำ)
3.  **Poll Station Terminal:** อุปกรณ์ ณ หน่วยเลือกตั้งที่ต้องทำการยืนยันตัวตนผ่านแอปฯ **ThaiD** หรือเครื่องอ่านบัตรประชาชน (Smart Card Reader) ก่อนการส่งโหวต เพื่อป้องกันการสวมสิทธิ์
4.  **Validator Nodes:** โหนดตรวจสอบอิสระ (รันโดยประชาชน, สื่อ, NGO)
5.  **Frontend Web App:** หน้ากากสำหรับประชาชน ณ หน่วยเลือกตั้ง และหน้าตรวจสอบสำหรับประชาชนทั่วประเทศ

---

## 🚀 ทางเลือกที่ 1: การรันเพื่อการพัฒนา (All-in-One Development)
หากคุณต้องการทดสอบทุกอย่างในเครื่องเดียว เราเตรียม Docker Compose ไว้ให้แล้ว:

```bash
# 1. โคลนโปรเจกต์
git clone https://github.com/GridsMicro/Open-Vote.git

# 2. เริ่มต้นระบบทั้งหมด (Blockchain, Redis, Aggregator, Validators)
docker compose up -d

# 3. รัน Frontend ใน Terminal แยก
cd frontend
npm install
npm run dev
```
*ระบบจะพร้อมใช้งานที่ `http://localhost:3000`*

---

## 独立 💡 ทางเลือกที่ 2: การแยกส่วนติดตั้ง (Independent Deployment)

### 1. การตั้งค่า Blockchain (Hardhat/Polygon)
หากรันเองในเครื่อง:
```bash
# ในโฟลเดอร์หลัก
npx hardhat node
```
*ในโลกจริง: เราจะใช้ RPC URL ของ Polygon Network*

### 2. การตั้งค่าส่วน Aggregator (ผู้จัดการเลือกตั้ง)
ต้องการ Node.js และ Redis:
```bash
cd aggregator
npm install
# ตั้งค่า .env (PORT, REDIS_URL, RPC_URL, CONTRACT_ADDRESS, PRIVATE_KEY)
npm run build
npm start
```

### 3. การติดตั้ง Validator Node (สำหรับสื่อและประชาชน)
รันง่ายๆ ผ่าน Docker Image:
```bash
docker run -e RPC_URL=... -e AGGREGATOR_URL=... gridsmicro/open-vote-validator
```

---

## 🔒 ความปลอดภัยและการบำรุงรักษา
*   **Private Keys:** ห้ามฝัง Private Key ลงในโค้ดเด็ดขาด ให้ใช้ Environment Variables/Secrets เท่านั้น
*   **Database:** ข้อมูลใน Redis คือข้อมูลชั่วคราว ข้อมูลที่ "ความจริงสูงสุด" จะอยู่ใน Blockchain และ Merkle Tree Storage เท่านั้น

> **"รันโหนดของคุณเอง เพื่อพิทักษ์ความถูกต้องของการเลือกตั้ง"**
